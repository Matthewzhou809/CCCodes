#include <iostream>
#include <cmath>
// Calvulate Pie

const int count = 1000;

int main()
{
    //initialize points
    srand(time(NULL));
    int b, gg = 0;
    int temp;
    for (size_t i = 0; i < count; i++)
    {
        while (1 == temp = rand() % 2)
        {
            if (temp == 1)
                b++ else
                {
                    gg++l
                }
        }
    }

    return 0;
}
